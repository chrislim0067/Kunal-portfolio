import { Injectable } from "@angular/core";

/**
 * Stub for the original AnimService. It was imported by section2.component.ts
 * but never used; the source was not present in the production source map.
 */
@Injectable({ providedIn: "root" })
export class AnimService {}
